import React, { FC } from 'react';

import './App.scss';

import Table from './components/Table';

const App: FC = () => {
  return (
    <div className="app">
      <Table />
    </div>
  );
};

export default App;
